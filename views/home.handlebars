<h1>List of Employees</h1>

<ul>
    {{#each employees}}
        <li>
            <a href="/employee/{{this._id}}">{{this.name}}</a>
        </li>
    {{/each}}
</ul>

<a href="/logout" class="btn">Logout</a>